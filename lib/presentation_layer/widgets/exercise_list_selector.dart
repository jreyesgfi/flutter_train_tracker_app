import 'package:flutter/material.dart';

class ExerciseListSelector extends StatelessWidget {


  const ExerciseListSelector({
    super.key
  });

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);

    return Container(
      
    );
  }
}